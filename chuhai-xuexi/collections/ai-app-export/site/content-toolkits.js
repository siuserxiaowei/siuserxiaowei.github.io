"use strict";

(function attachPromptAssetToolkits() {
  const content = window.learningHubContent || {};
  const lastVerifiedDate = content.lastVerifiedDate || "2026-06-02";
  const toolkits = Array.isArray(content.toolkits) ? content.toolkits : [];

  const promptAssetToolkits = [
    {
      id: "customer-input-form",
      title: "客户输入表",
      summary: "承接前同事沉淀的客户访谈经验，把输入字段先脱敏、再归档，保证后续提示词调用有稳定上下文。",
      template: `客户输入表

项目/需求名称：
记录人：
记录日期：
客户已脱敏编号：

一、客户目标
客户希望达成的结果：
当前使用的流程或工具：
必须保留的业务约束：
不能触碰的风险边界：

二、输入材料
客户提供的材料类型：
已完成脱敏的字段：
仍需客户确认的字段：
缺失信息与补问计划：

三、人工判断
前同事经验资产中可复用的判断规则：
本次需要人工复核的重点：
交付前负责人：
复核结论：`,
      checks: [
        "所有姓名、联系方式、账号、订单、合同、截图等敏感信息已脱敏。",
        "客户目标写成可验收结果，而不是泛泛的功能愿望。",
        "输入缺口已经列明，并区分必须补问和可延后确认。",
        "保留前同事沉淀的判断规则来源，便于流程传承。",
        "交付前安排人工复核人，不让模型输出直接替代专业判断。"
      ],
      verified: lastVerifiedDate
    },
    {
      id: "prompt-original-register",
      title: "提示词原件登记表",
      summary: "尊重原始提示词和背后的业务经验，先登记版本、用途和边界，再开展改写、拆解或自动化。",
      template: `提示词原件登记表

原件名称：
登记人：
登记日期：
来源说明：
适用业务场景：

一、原提示词
原文粘贴区：

二、经验说明
前同事设计该提示词的目的：
它依赖的客户输入字段：
它隐含的业务判断：
它要求模型避免的内容：

三、版本管理
当前版本编号：
允许修改的部分：
禁止修改的部分：
修改前需要确认的人：

四、复核记录
脱敏检查结果：
人工复核意见：
是否可进入测试：`,
      checks: [
        "原文完整保留，并和后续改写版本分开登记。",
        "来源、用途、适用场景写清楚，避免经验资产被误用。",
        "客户材料或案例描述不包含可识别个人或企业隐私。",
        "标注哪些内容是前同事的核心判断，修改前需复核。",
        "每次改写都有版本号和人工复核记录。"
      ],
      verified: lastVerifiedDate
    },
    {
      id: "output-quality-checklist",
      title: "输出质检表",
      summary: "把好输出的标准写成可执行检查项，让新人能继承既有交付口径，同时保留人工复核的最后一道关。",
      template: `输出质检表

项目/输出名称：
质检人：
质检日期：
关联提示词版本：
关联客户脱敏编号：

一、结果核对
输出是否回应客户目标：
关键事实是否需要再次核查：
是否存在过度承诺：
是否有遗漏的限制条件：

二、表达核对
语气是否尊重、清楚、可执行：
是否符合团队既有交付口径：
是否保留必要的风险提示：
是否避免暴露内部或客户隐私：

三、人工复核
需要人工改写的段落：
需要升级给负责人的问题：
最终结论：通过 / 修改后通过 / 不通过
复核备注：`,
      checks: [
        "输出先对照客户目标验收，不只看文字是否流畅。",
        "事实、日期、价格、法规、政策、平台规则等易变信息已核查或标注待核查。",
        "没有承诺收益、替代专业判断或隐藏重要风险。",
        "语气延续团队经验资产中的交付风格，尊重客户和前序工作。",
        "人工复核意见已记录，不能只依赖模型自评。"
      ],
      verified: lastVerifiedDate
    },
    {
      id: "failure-sample-replay",
      title: "失败样本回放表",
      summary: "用失败样本保护经验资产，把问题复盘成边界、补问和质检规则，而不是简单归因给某一句提示词。",
      template: `失败样本回放表

失败样本编号：
记录人：
回放日期：
关联提示词版本：
关联客户脱敏编号：

一、失败现象
客户原始目标摘要：
模型输出摘要：
不符合预期的部分：
造成影响：

二、原因分析
输入信息是否不足：
提示词边界是否不清：
业务判断是否缺失：
是否需要人工升级处理：

三、修复动作
需要补充的客户输入字段：
需要新增的拒答或风险提示：
需要加入输出质检表的检查项：
需要更新的提示词版本：

四、复核结论
回放负责人：
修复后是否通过：
后续观察计划：`,
      checks: [
        "失败样本已脱敏，不能用真实客户隐私做训练或展示。",
        "复盘同时检查输入、提示词、业务判断和人工复核，不单点归因。",
        "每个失败原因都转化为补问字段、边界提示或质检项。",
        "涉及高风险或专业判断的样本已升级人工负责人复核。",
        "修复动作和提示词版本变更可追溯，便于团队传承。"
      ],
      verified: lastVerifiedDate
    }
  ];

  const existingIds = new Set(toolkits.map((toolkit) => toolkit.id));
  const appendedToolkits = promptAssetToolkits.filter((toolkit) => !existingIds.has(toolkit.id));

  window.learningHubContent = {
    ...content,
    lastVerifiedDate,
    toolkits: [...toolkits, ...appendedToolkits]
  };
})();
