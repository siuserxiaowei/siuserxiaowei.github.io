# 赛事雷达第二轮：自主执行计划与 50 个检查点

更新时间：2026-07-30  
基线：184 条机会 / 赛题记录，183 个赛事系列；本轮合并后 202 条；线上 JSON schema 2.0。

## 执行契约

- 模式：Managed Execution，自治等级 L4。
- 目标：重新检索仍可报名或即将开放的软件、AI、Agent、算法、开源、游戏、创意与创业机会，同时复核旧数据并降低主雷达页面成本。
- 可直接执行：公开信息检索、本地读写、分支与 worktree、测试、构建、浏览器验收和用户已明确授权的生产部署。
- 委派：只并行互不依赖的三条线；新增数据、旧数据更正、前端性能文件所有权互斥。
- 主 Agent 责任：检查来源、审阅 diff、运行独立测试、决定是否合并、解决共享集成点、回收 worktree。
- 暂停条件：需要新凭证、付费、删除用户数据、修改无关系统或事实证据否定核心方向。
- 重试规则：同类失败最多进行 3 轮有证据的修复；不能用重复尝试代替诊断。

## 真实完成标准

1. 新增记录必须有官方或主办方一手来源，且不与现有 184 条重复。
2. 截止日期必须区分 confirmed、estimated、rolling、unknown；后三者不得进入紧急倒计时或 ICS。
3. 旧数据复核至少覆盖全部 urgent 和全部已标 verified / partially-verified 记录。
4. 主雷达仍保留搜索、筛选、排序、URL、收藏、空状态、ICS、移动端和无障碍能力。
5. 构建后的赛事首页目标小于 1.2 MB，DOM 元素目标小于 8,000。
6. 数据、链接安全、构建、桌面、移动端与生产环境均有可复核证据。
7. 子 Agent 报告、仅有计划、搜索摘要或未运行验证均不算完成。

## 50 个证据化检查点

| # | 检查点 | 状态 | 验收证据 |
|---:|---|---|---|
| 01 | 创建第二轮可追踪目标 | 通过 | Codex goal 已建立 |
| 02 | 先制定完整计划 | 通过 | 当前文件与任务计划已创建 |
| 03 | 采用 L4 自主执行契约 | 通过 | 明确授权、边界、暂停和完成规则 |
| 04 | 主分支洁净检查 | 通过 | `main` 基线 `7f2fbd8`，无未提交文件 |
| 05 | worktree 基线检查 | 通过 | 初始仅主工作区 |
| 06 | 本地记录总量 | 通过 | 184 条 |
| 07 | 去重后的赛事系列量 | 通过 | 183 个 series / parent / standalone key |
| 08 | 线上 JSON 一致性 | 通过 | schema 2.0、184 条、updatedAt 2026-07-30 |
| 09 | 状态分布基线 | 通过 | urgent 37、ongoing 89、upcoming 26、unknown 9、expired 23 |
| 10 | 核验状态基线 | 通过 | verified 15、partially-verified 2、unverified 167 |
| 11 | 日期可信度基线 | 通过 | confirmed 175、estimated 5、rolling 3、unknown 1 |
| 12 | 现有自动化基线 | 通过 | 13/13 数据测试、5/5 链接安全测试、P0 为 0 |
| 13 | 新增赛事 worktree | 通过 | `feat/radar-round2-additions` 已审查并合入 |
| 14 | 旧数据复核 worktree | 通过 | `feat/radar-round2-audit` 已审查、合入并回收 |
| 15 | 页面性能 worktree | 通过 | `feat/radar-round2-performance` 已审查并合入 |
| 16 | 新增候选池不少于 25 条 | 通过 | 46 条候选：18 采用、13 待定、15 排除 |
| 17 | 新增记录与 184 条去重 | 通过 | 合并后 202 条，ID 全局唯一；新增 18 条各出现一次 |
| 18 | 新增记录资格边界 | 通过 | 地区、身份、团队、费用、企业阶段均为结构化必检项 |
| 19 | 新增记录奖金边界 | 通过 | cash / credits / investment / travel 结构化分离并测试 |
| 20 | 新增记录多截止节点 | 通过 | registration / submission / opening 分离；主截止均有来源 |
| 21 | 全部 urgent 复核 | 通过 | 37/37；修正后 urgent 34 |
| 22 | 全部已核验记录再复核 | 通过 | 17/17；与 urgent 去重后 50 条 claim review |
| 23 | 其余 active 风险抽样 | 通过 | 全量 184 记录 / 210 URL，另含 44 条重点 correction map |
| 24 | 死链与失效入口处理 | 通过 | OpenHarmony / MiraclePlus 均替换为可达官方入口 |
| 25 | 已过期与假紧急检查 | 通过 | 11 estimated、1 unknown、4 rolling 均不进紧急倒计时 |
| 26 | 父子赛题与总量口径 | 通过 | Pokémon series；Wearable / FightingICE / CoG track |
| 27 | 构建复核队列命令 | 通过 | 更正与新增后 `npm run radar:review` 输出 142 条风险队列 |
| 28 | 复核队列单元测试 | 通过 | 6/6；含优先级、日期可信度、死链、排序、CLI 与本地日期 |
| 29 | 页面 HTML 体积基线 | 通过 | 生产构建 `2,170,134` bytes |
| 30 | 页面 DOM 基线 | 通过 | 静态 HTML 粗计 `21,863` 个元素 |
| 31 | 单详情容器或等效减重 | 通过 | 202 条列表共用 1 个详情容器 |
| 32 | 搜索字段完整性 | 通过 | 浏览器以 `FutureEval` 检索到唯一结果及完整详情 |
| 33 | 分类/等级/状态/排序 | 通过 | 组合筛选保留；`unknown` 状态可筛出 16 条且全部真实为 unknown |
| 34 | URL 状态与分享 | 通过 | 搜索、状态和当前赛事同步到 query/hash |
| 35 | 浏览器前进后退 | 通过 | 性能分支桌面/移动回归已覆盖 popstate 恢复 |
| 36 | 收藏持久化 | 通过 | localStorage 读写、坏 JSON 降级与刷新保留完成浏览器回归 |
| 37 | 只看收藏与清空 | 通过 | 正常、零结果、恢复完成浏览器回归 |
| 38 | ICS 可信度边界 | 通过 | unknown 筛选下日历按钮全部禁用；节点标签有单测 |
| 39 | 无 JS 基本可用性 | 通过 | 202 条 SSR 列表各保留独立详情链接 |
| 40 | HTML 小于 1.2 MB | 通过 | 202 条构建为 `866,032` bytes |
| 41 | DOM 小于 8,000 | 通过 | 构建静态 HTML 粗计 `3,914` 个元素 |
| 42 | 数据 schema 与唯一性测试 | 通过 | 41/41 数据与页面预算测试通过 |
| 43 | 链接安全测试 | 通过 | SSRF/TLS/redirect 边界测试随全量测试通过 |
| 44 | 分批真实外链审计 | 通过 | 全库 232 URL：210 ok、5 bot-blocked、0 dead、17 uncertain |
| 45 | 生产构建 | 通过 | Astro 共生成 278 个页面 |
| 46 | 桌面浏览器验收 | 通过 | 搜索、unknown、详情切换、URL、控制台零错误 |
| 47 | 移动与无障碍验收 | 通过 | 390×844 无横向溢出；ARIA 控件可操作；详情宽 334px |
| 48 | worktree 合入与回收 | 通过 | 三个分支均经主线复核合入；仅保留 `main` 工作区 |
| 49 | 生产部署与线上冒烟 | 通过 | 主域、Pages 部署、202 条 JSON、FutureEval 详情与 llms.txt 均 HTTP 200 |
| 50 | 更新闭环与交接 | 通过 | 每日 08:30 安全监控运行成功；生产部署 `03abc6f8` 已上线 |
