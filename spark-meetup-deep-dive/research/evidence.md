# Evidence Cards Guide

机器可审计证据位于 `evidence_cards.tsv`。本次独立 worker 先完成实体/方法材料收集，主任务随后直接复读核心一手页面并执行 curator acceptance。

- `accepted` 只表示“该来源支持该窄命题”，不表示命题可无限外推。
- 创始人收入卡即便接受，也只能表述为“本人公开页面自报”。
- `single_source` 与 `low/medium confidence` 在报告中必须带限制。
- 搜索摘要未作为独立证据；能力失败与缺口保留在 run manifest 和 gap backlog。
